services:
  loki:
    image: grafana/loki:latest
    container_name: loki
    network_mode: host
    command: -config.file=/etc/loki/loki-config.yaml
    volumes:
      - ./configs/loki/generated/loki-local-dev.yaml:/etc/loki/loki-config.yaml:ro
      - loki-data:/loki
    restart: unless-stopped
    logging:
      driver: fluentd
      options:
        fluentd-address: localhost:24224
        fluentd-async: "true"
        fluentd-buffer-limit: "1048576"
        tag: docker.{{.Name}}
  fluent-bit:
    image: fluent/fluent-bit:latest
    container_name: fluent-bit
    network_mode: host
    command: ["/fluent-bit/bin/fluent-bit", "-c", "/fluent-bit/etc/fluent-bit.yaml"]
    depends_on:
      - loki
    environment:
      - LOKI_HOST=localhost
      - LOKI_PORT=3100
      - ENVIRONMENT=${ENVIRONMENT:-production}
      # Use actual machine hostname (not container hostname)
      - HOSTNAME
    volumes:
      - /var/log/journal:/var/log/journal:ro
      - /run/log/journal:/run/log/journal:ro
      - /run/systemd/journal:/run/systemd/journal:ro
      - /run/user/1000/systemd/journal:/run/user/1000/systemd/journal:ro
      - /etc/machine-id:/etc/machine-id:ro
      - ./configs/fluent_bit/fluent-bit.yaml:/fluent-bit/etc/fluent-bit.yaml:ro
      - ./configs/fluent_bit/parsers.conf:/fluent-bit/etc/parsers.conf:ro
      - ./configs/fluent_bit/scripts:/fluent-bit/etc/scripts:ro
      - fluent-bit-state:/var/log
    user: "0"  # Run as root to access system journal
    cap_add:
      - CAP_DAC_READ_SEARCH  # Capability to bypass file read permission checks
    restart: unless-stopped
    # Use json-file for fluent-bit's own logs to avoid chicken-and-egg problem
    # (fluent-bit can't send logs to itself during startup)
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    network_mode: host
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GF_ADMIN_PASSWORD:-admin}
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-data:/var/lib/grafana
      - ./configs/grafana-datasources.yaml:/etc/grafana/provisioning/datasources/loki.yaml:ro
    restart: unless-stopped
    logging:
      driver: fluentd
      options:
        fluentd-address: localhost:24224
        fluentd-async: "true"
        fluentd-buffer-limit: "1048576"
        tag: docker.{{.Name}}
    depends_on:
      - loki
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

volumes:
  grafana-data:
    driver: local
  loki-data:
    driver: local
  fluent-bit-state:
    driver: local
